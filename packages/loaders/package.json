{
  "name": "@claricore/loaders",
  "version": "0.4.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "migrations"
  ],
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@claricore/core": "workspace:*",
    "@claricore/observability": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "vitest": "^2.1.2"
  }
}
